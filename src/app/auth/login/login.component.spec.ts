/* tslint:disable:no-unused-variable */

import { LoginComponent } from './login.component';

describe('Component: Fake Login', () => {
  it('should create an instance', () => {
     //let component = new LoginComponent();
     //expect(component).toBeTruthy();
  });
});
